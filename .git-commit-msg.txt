Tailor the example demo to its real audience: SEs/CSAs showcasing Microsoft services

Two changes, both from direct feedback:

1. Dropped the "Northwind Tasks isn't real / not a real product" disclaimer framing
   entirely (DISCLAIMER export removed, closing scene rewritten). That pattern was
   carried over from the Deal Room demos, where it does real work (this dataset isn't
   real customer data); it added nothing here and just apologized for the sample app.

2. This skill's actual primary audience is Solutions Engineers and CSAs demoing what a
   Microsoft service (Foundry, Fabric, or similar) can do inside a customer's product —
   not a generic task tracker with no point of view. Rebuilt the sample app and all 9
   scenes (was 7) around two concrete, named integrations instead of generic filler:
   - Azure AI Foundry: an agent-written weekly briefing on the dashboard, and an
     "Ask Copilot" Q&A panel on the task detail page (one checklist item tagged as a
     Copilot suggestion).
   - Microsoft Fabric: a velocity-trend chart on the dashboard, and a capacity-forecast
     callout on the team page.

Re-ran the full pipeline end to end: 9/9 scenes captured, 18 audio files synthesised,
player + 2m29s/2.0MB MP4 built. Re-imported into Amit's Demos (catalog summary/tags/
resource durations updated to match), 17/17 tests, republished.
